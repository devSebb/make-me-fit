<div class="container mx-auto my-8 p-6 shadow-lg rounded-lg bg-white w-full md:w-1/3" style="box-shadow: 0 0 15px 5px #064e3b;">
  <h2 class="text-2xl font-bold text-center text-emerald-700 mb-4">Sign up</h2>
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field mb-4">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "rounded-lg focus:ring-2 focus:ring-emerald-800 w-full" %>
    </div>

    <div class="field mb-4">
      <%= f.label :password %>
      <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> characters minimum)</em>
      <% end %><br />
      <%= f.password_field :password, autocomplete: "new-password", class: "rounded-lg focus:ring-2 focus:ring-emerald-800 w-full" %>
    </div>

    <div class="field mb-4">
      <%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "rounded-lg focus:ring-2 focus:ring-emerald-800 w-full" %>
    </div>

    <div class="actions text-center">
      <%= f.submit "Sign up", class: "transition duration-300 ease-in-out bg-transparent text-emerald-700 border border-emerald-700 hover:bg-emerald-800 hover:text-white py-2 px-4 rounded-lg" %>
    </div>
  <% end %>
  <div class="text-center flex flex-col ">or
    <%= render "devise/shared/links" %>
  </div>
</div>
