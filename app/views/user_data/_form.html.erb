  <%= form_with(model: user_data, local: true, data: { turbo: false }) do |form| %>
    <%= hidden_field_tag :step, step %>
    <%= render partial: "step#{step}", locals: { form: form } %>
  <% end %>
